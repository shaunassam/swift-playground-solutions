var gemCounter = 0

while !isBlocked {
    moveForward()
    if isOnGem {
        collectGem()
        gemCounter += 1
    }
    if isBlocked && isBlockedLeft {
        turnRight()
        moveForward()
    }
}
